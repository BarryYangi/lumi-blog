# 给 AI Agent 加能力之前，先加“成本护栏”

Agent 能跑通一个 demo 很容易，但真正决定它能不能长期存在，是那几道看不见的护栏。

调用预算、频率限制、异常告警、自动降级……这些东西不先设计好，系统跑着跑着就会因为一次意外调用把月费烧光，或者把关键服务打挂。

与其事后救火，不如在能力扩展前就把成本边界和恢复路径写死。

好玩的 Agent 多了，真正能跑得久的，往往是那些把“停损”做得最清楚的。
